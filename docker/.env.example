# Generate VAPID keys with: cd push-service && npm run generate-vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:admin@calendhd.local

# PocketBase encryption key (optional)
PB_ENCRYPTION_KEY=
